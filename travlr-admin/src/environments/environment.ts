// Development environment (default — used when no fileReplacement applies).
// Edit `apiUrl` to point at a non-local backend during development.
export const environment = {
    production: false,
    apiUrl:     'http://localhost:3000/api',
    publicUrl:  'http://localhost:3000'
};
