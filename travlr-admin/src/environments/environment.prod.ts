// Production environment — replaces environment.ts at build time when
// the `production` configuration is selected (see angular.json fileReplacements).
export const environment = {
    production: true,
    apiUrl:     '/api',
    publicUrl:  ''
};
